import { useState } from "react"

const Calander = ({ events }) => {

    const [state, setState] = useState([])

    return (
        <div>
            Calander
        </div>
    )
}

export default Calander
